import type { Event } from "@/utils/data/types";

export function Event(props: Event) {
    return (
        <div>
            some photo(s)
            descrtiption
            title
        </div>
    )
}
